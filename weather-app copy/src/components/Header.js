import React from "react"

const Header = () => (
  //visual formatting of header can be found in styles folder (based on className)
  <div className="header">
    <h1 className="header__title">Forecast Finder</h1>
  </div>
)
export default Header
